# Enable bean definition overriding only for tests to avoid duplicate bean name conflicts
spring.main.allow-bean-definition-overriding=true

# Disable Flyway migrations during tests (migrations depend on production DB state)
spring.flyway.enabled=false

# Let Hibernate create and drop the schema for tests
spring.jpa.hibernate.ddl-auto=create-drop
